# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Next.js
.next/
out/
build/
dist/

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# IDE and Editor files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs
logs/
*.log

# Runtime data
pids/
*.pid
*.seed
*.pid.lock

# Coverage directory used by tools like istanbul
coverage/
*.lcov

# nyc test coverage
.nyc_output

# ESLint cache
.eslintcache

# Optional npm cache directory
.npm

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# parcel-bundler cache (https://parceljs.org/)
.cache
.parcel-cache

# Temporary folders
tmp/
temp/

# Test artifacts
.vercel-test-deploy.txt

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local development files
*.local

# API Keys and sensitive files (if any get accidentally committed)
**/api-keys.json
**/secrets.json

# AI context files
CLAUDE.md
.claude/

# Old/temporary analysis files
analyze.js
generate-daily-post-auto.js

# Analysis output (generated data, can be regenerated)
analysis/output/

# Historical census data - stored in data-archive branch (permanent Git storage)
# Main branch only tracks census-data-latest.json to keep deployments fast
#
# Architecture:
# - data-archive branch: ALL historical dated files (permanent, no expiration)
# - main branch: Only census-data-latest.json (for Vercel deployment)
# - Local access: Use 'git worktree add archive data-archive' for all historical files
# - Auto-sync: LaunchAgent runs daily at 3 AM to sync archive branch locally
#
# This eliminates the 30-day artifact expiration risk while keeping main branch clean
public/data/etoro-data-*.json
public/reports/etoro-census-*.html

# Archive worktree directory (created by sync script)
archive/

# Keep the latest file tracked for Vercel (negation pattern)
!public/data/census-data-latest.json

# Private keys and certificates
*.pem
*.key
*.p12
*.pfx
